package service

func save() {
	//file, err := os.Create("data/questsMap.dat")
	//tools.CheckErr(err)
	//defer file.Close()
	//encoder := gob.NewEncoder(file)
	//err = encoder.Encode(QuestsMap)
	//tools.CheckErr(err)
}
func read() {
	//file, err := os.Open("data/goods.dat")
	//tools.CheckErr(err)
	//defer file.Close()
	//// 创建一个解码器
	//decoder := gob.NewDecoder(file)
	//// 解码数据并存储到新的切片
	//err = decoder.Decode(&NewGoodsSlice)
	//tools.CheckErr(err)
}
